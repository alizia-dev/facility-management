/**
 * Where the API lives.
 *
 * A constant rather than an Angular environment file: this project has one
 * deployment target (a developer's machine) and adding a second environment file
 * to hold one string would be ceremony. In production this becomes a build-time
 * environment value or a runtime config fetched before bootstrap.
 */
export const API_BASE_URL = 'http://localhost:5000/api';
